# Deployment target
DEPLOY_HOST=example.com
DEPLOY_USER=username
DEPLOY_PORT=22
DEPLOY_PATH=/home/username/public_html/history-view

# Optional: API keys or rate-limit knobs for favicon fetching
HTTP_TIMEOUT=10
HTTP_CONCURRENCY=4
